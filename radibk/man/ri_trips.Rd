% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trips.R
\name{ri_trips}
\alias{ri_trips}
\alias{c.ri_trips}
\alias{plot.ri_trips}
\title{Calculating Bike Trips}
\usage{
ri_trips(x, cores = NULL, verbose = FALSE, ...)

\method{c}{ri_trips}(..., cores = NULL)

\method{plot}{ri_trips}(x, n = 15L, ...)
}
\arguments{
\item{x}{object of class \code{ri_data} as returned by
\code{\link[=ri_read_jsons]{ri_read_jsons()}}, \code{\link[=ri_read_zip]{ri_read_zip()}}, or \code{\link[=ri_read_zips]{ri_read_zips()}}.}

\item{cores}{\code{NULL} (default) or integer, not used by \code{ri_read_json}.
Number of cores to used with \code{parallel::mclapply}. If \code{NULL} it takes all but
two available cores (topped of by the maximum number of json files to be
processed).}

\item{verbose}{defaults to \code{FALSE}.}

\item{...}{S3 method \code{c.tp_trips}: one or multiple \code{ri_trip}
objects to be combined. They must have the same time zone and must not
overlap, else an error is thrown. For S3 method \code{plot.tp_trips}: forwarded
to barplot function.}

\item{n}{positive integer, defaults to 15L.}
}
\value{
Object of class \code{c("ri_trips", "tbl", "data.frame")}
containing bike trips as well as 'open trips' (see below).
Besides the bike \code{number} (the identifier) the data frame
contains \verb{*_place_id}, \verb{*_daetetime}, \verb{*_name}, \verb{*_lng}, and \verb{*_lat}
for both the start and the end of the trip. In addition, the trip
duration is reported in minutes (rounded to closest minutes).

Besides closed trips (where we know the start and end) the data frame
also contains "open trips". These are always added at the start of
the data period (with respect to 'datetime') and end of the data period
and are required to capture potential bike trips which started before
the earliest observation into the period the data covers, as well
as trips starting within the data period but have not finished at
the end of the period.
These are later used when combining multiple \code{ri_trips} objects
(see S3 method \code{c.ri_trips}).

The \code{c.ri_trips} method allows to combine multiple objects of this class.
Will also resolve potential open trips.
}
\description{
Uses the raw data to calculate bike trips, i.e., calculate
when a bike disapeared at a certain location and then popped up
at another one (considered a rented trip).
}
\author{
Reto
}
